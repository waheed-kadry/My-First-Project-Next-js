import React from 'react'
export const metadata = {
    title: "Articles Route"
}

function Layout_Articles({children}) {
  return (
    <div>
      Layout_Articles
      {children}
    </div>
  )
}

export default Layout_Articles
